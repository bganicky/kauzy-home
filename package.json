{
  "name": "kauzy_home",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "^3.4.0"
  },
  "dependencies": {
    "dms-conversion": "^2.0.1",
    "react": "^15.3.2",
    "react-dom": "^15.3.2",
    "smoothscroll-polyfill": "^0.3.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "rsync -a build/ web567:webapps/kauzy_home",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
